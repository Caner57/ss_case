# Build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
x64/
x86/
[Bb]uild/

# .NET
*.user
*.suo
project.lock.json
.vs/
.vscode/

# Rider / JetBrains
.idea/

# Node / React (Admin.Web)
node_modules/
dist/
.npm/

# OS
.DS_Store
Thumbs.db

# ---- Secrets (CFG-9.4) ----
# Never commit real secrets. Local overrides and env files stay untracked.
# .env is intentionally tracked: it holds local-dev-only placeholder values so a
# fresh clone can `docker compose up` without manually recreating it.
appsettings.*.local.json
appsettings.Local.json
.env.*
secrets.json
*.pfx
*.key
