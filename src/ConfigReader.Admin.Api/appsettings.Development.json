{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "_comment_secrets": "Local, non-production defaults only, matching docker-compose (.env.example). These credentials are harmless local dev values, never production secrets.",
  "ConnectionStrings": {
    "Mongo": "mongodb://configuser:local-dev-password@localhost:27017/configdb?authSource=admin"
  },
  "Redis": {
    "ConnectionString": "localhost:6379"
  },
  "AdminApi": {
    "ApiKey": "local-dev-admin-key",
    "AllowedOrigins": [ "http://localhost:5173" ]
  }
}
